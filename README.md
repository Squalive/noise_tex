A rust library utilizing the `noise` crate to generate noise textures.

This crate allows you to generate textures with different channels pack with different noise data. Which is useful when you want to pack multiple different noise into one filterable texture with different channels.
